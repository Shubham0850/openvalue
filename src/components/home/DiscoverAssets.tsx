import { Container, Box, Text, Flex } from "@chakra-ui/react";
import React from "react";

function WhyUs() {
  return (
    <Container maxW={1300} p={{ base: "5vh 0px", md: "15vh 0px" }}></Container>
  );
}

export default WhyUs;

// make a section DiscoverAssets containing a heading and a para below .. and 6 icons each with a heading below use react icons for that six icons... see each line will contain 3 icons means 2 lines of icons ... make it
